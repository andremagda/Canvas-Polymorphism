#include "Quadrilateral.h"

// Use Shape’s constructor to initialize inherited members
Quadrilateral::Quadrilateral(int width, int height, const std::string& name)
    : Shape(width, height, name) {
    // Additional initialization if needed
}